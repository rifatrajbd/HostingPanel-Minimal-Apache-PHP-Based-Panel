# /etc/fail2ban/filter.d/hostingpanel.conf
# Matches failed panel logins written by the Laravel auth listener to
# /var/log/hostingpanel/auth.log
[Definition]
failregex = ^.*panel login failed from <HOST>.*$
ignoreregex =
